# get-weather-4

New plugin scaffold

## Commands
- Place markdown files in `./commands`.

## Agents
- Place agent markdown files in `./agents`.

## Skills
- See `./skills/sample-skill/SKILL.md` for a starter.

## Hooks
- Configure hooks in `./hooks/hooks.json`.

## MCP Servers
- Configure servers in `./.mcp.json`.
